import { connect } from "react-redux";
import DoseCard from "./DoseCard";

const mapStateToProps = state => {
    return {
  
        
    };
};

export default connect(mapStateToProps)(DoseCard);